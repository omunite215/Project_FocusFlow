/**
 * ProgressRing — base UI component.
 * TODO: implement with Tailwind styling and accessibility.
 */
export default function ProgressRing({ children, ...props }) {
  return <div {...props}>{children}</div>;
}
